apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: targeting-service
spec:
  selector:
    app: targeting-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8003